<header class="card-header d-flex justify-content-between">
    <strong>ADMIN</strong>
    <nav>
        <a href="{{ route("categories.index") }}" class="mx-1">Catégories</a>
        <a href="{{ route('products.index') }}">Produits</a>
    </nav>
</header>
